name: Build and deploy
on:
  push:
    branches:
    - test-runner
jobs:
 run:
  runs-on: macos-12
  steps:
  - uses: actions/checkout@v1
  - name: script
    run: bash ${GITHUB_WORKSPACE}/naisdevice-nav-mac-up.sh